# ─── Ollama (Windows Official Installer) ──────────────────
# Default path for official Ollama Windows installer
# Change only if you installed to a custom location
OLLAMA_BIN=D:\Program Files\Ollama\ollama.exe
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# ─── Ollama Models Folder (Windows) ───────────────────────
# Where Ollama stores downloaded models on Windows
OLLAMA_MODELS=D:\Program Files\Ollama\models

# ─── Telegram ─────────────────────────────────────────────
# 1. Open Telegram → search @BotFather
# 2. Send /newbot → follow steps
# 3. Paste the token below
TELEGRAM_TOKEN=8520453501:AAHx5BG8EPeZIiZGow9PeQZbKGysDpQ_x-o

# ─── REST API ─────────────────────────────────────────────
# Change this to something secret!
API_SECRET_KEY=mysecretkey123

# ─── Server ───────────────────────────────────────────────
HOST=0.0.0.0
PORT=7777

# ─── Free-tier AI Provider (OpenAI-compatible API) ─────────
# Example providers: OpenRouter, Together, Groq (use each provider's base URL)
FREE_AI_ENABLED=false
FREE_AI_BASE_URL=https://openrouter.ai/api/v1
FREE_AI_API_KEY=your_free_provider_api_key
FREE_AI_MODEL=meta-llama/llama-3.1-8b-instruct:free
FREE_AI_TIMEOUT=60
USE_FREE_AI_FALLBACK=true

# ─── Project Task Runner (opencode / cursor) ───────────────
PROJECT_DIR=C:\_IMPACT\tomcat\webapps\impact_vite
OPENCODE_CMD_TEMPLATE=& "D:\Program Files\OpenCode\OpenCode.exe" "{project_dir}"
CURSOR_CMD_TEMPLATE=cursor "{project_dir}"

PROJECT_ROOT=D:\PERSONAL\LIVE_PROJECTS
ALLOWED_TELEGRAM_USER_ID=123456789